// API configuration
export const API_BASE_URL = window.location.hostname === 'localhost' 
  ? 'http://localhost:3003' 
  : window.location.origin;

// Other configuration options can be added here
export const CLIENT_PORT = 3004;
